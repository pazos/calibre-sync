buildscript {
    ext.gradle_plugin_version = '3.5.3'
    ext.minSdk = 14
    ext.targetSdk = 21

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath "com.android.tools.build:gradle:${gradle_plugin_version}"
    }
}
